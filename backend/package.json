{
  "name": "rrr",
  "version": "1.0.0",
  "description": "Romanian Rat Race",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc && npx prisma configure && (cd ../frontend && npm run build) && exit 1",
    "run": "npm run build && node build/index.js && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^4.16.2",
    "express": "^4.18.2",
    "sqlite3": "^5.1.6",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/uuid": "^9.0.2",
    "@types/ws": "^8.5.5",
    "prisma": "^4.16.2",
    "typescript": "^5.1.6"
  }
}
